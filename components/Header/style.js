import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    height: 80,
    paddingTop: 38,
    backgroundColor: '#cf000f',
  },
  title: {
    textAlign: 'center',
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold'
  }
});