import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    marginBottom: 10,
    marginHorizontal: 8,
    marginVertical: 6,
    borderBottomWidth: 1,
    borderBottomColor: '#ddd'
  }
});